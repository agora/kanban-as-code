# System Prompt Memory

Reusable prompts, patterns, and safeguards harvested from `kan loop --stage review` and `kan loop --stage compound`.

This file is append-only and intended for compounding engine ingestion.
