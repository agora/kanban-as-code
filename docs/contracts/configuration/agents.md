---
id: docs-contracts-configuration
type: generic
title: Agents Scope - docs/contracts/configuration
status: active
---

# Agents

Scope boundary: `docs/contracts/configuration`

Session contract:

- Start with `kan start --json`.
- Use `kan agent claim --scope docs/contracts/configuration` or claim by task when parallel edits are likely.
- Keep claims narrow and short-lived.
- If overlap risk appears, pause and re-claim with narrower scope.
