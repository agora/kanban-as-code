---
id: docs-architecture-patterns
type: generic
title: Agents Scope - docs/architecture/patterns
status: active
---

# Agents

Scope boundary: `docs/architecture/patterns`

Session contract:

- Start with `kan start --json`.
- Use `kan agent claim --scope docs/architecture/patterns` or claim by task when parallel edits are likely.
- Keep claims narrow and short-lived.
- If overlap risk appears, pause and re-claim with narrower scope.
