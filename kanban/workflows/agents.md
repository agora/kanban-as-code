---
id: kanban-workflows
type: generic
title: Agents Scope - kanban/workflows
status: active
---

# Agents

Scope boundary: `kanban/workflows`

Session contract:

- Start with `kan start --json`.
- Use `kan agent claim --scope kanban/workflows` or claim by task when parallel edits are likely.
- Keep claims narrow and short-lived.
- If overlap risk appears, pause and re-claim with narrower scope.
